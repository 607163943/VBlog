<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.sang.mapper.ColumnMapper">
    <select id="getAllCategories" resultType="org.sang.pojo.po.Column">
        SELECT * FROM columns
    </select>
    <!--专栏分页查询-->
    <select id="pageQuery" resultType="org.sang.pojo.po.Column">
        select * from columns
        <where>
            <if test="columnName!=null and columnName!=''">
                columnName like concat('%',#{columnName},'%')
            </if>
        </where>
    </select>
    <!--根据id查询专栏-->
    <select id="getColumnById" resultType="org.sang.pojo.po.Column">
        select * from columns where id = #{id}
    </select>
    <delete id="deleteColumnByIds">
        DELETE FROM columns WHERE id IN
        <foreach collection="ids" separator="," open="(" close=")" item="id">
            #{id}
        </foreach>
    </delete>
    <update id="updateColumn" parameterType="org.sang.pojo.po.Column">
        UPDATE columns
        <set>
            <if test="columnName!=null and columnName!=''">
                columnName=#{columnName},
            </if>
        </set>
        where id=#{id}
    </update>
    <insert id="addColumn" parameterType="org.sang.pojo.po.Column">
        INSERT INTO columns SET date=#{date},columnName=#{columnName}
    </insert>
</mapper>